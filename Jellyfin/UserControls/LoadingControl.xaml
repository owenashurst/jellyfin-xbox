<UserControl
    x:Class="Jellyfin.UserControls.LoadingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Jellyfin.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" Visibility="Collapsed"
    d:DesignWidth="400">

    <Grid Height="400" Width="650" Background="#B0000000" CornerRadius="5" Padding="100">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Loading..." FontSize="{StaticResource FontL}" HorizontalAlignment="Center" VerticalAlignment="Bottom" />
        
        <ProgressRing Grid.Row="1" IsActive="true" Height="80" Width="80" Foreground="LightSkyBlue"></ProgressRing>
    </Grid>
</UserControl>
